class Meal {
  constructor(id, menu, title, imageUrl, description, allergens) {
    this.id = id;
    this.menu = menu;
    this.title = title;
    this.imageUrl = imageUrl;
    this.description = description;
    this.allergens = allergens;
  }
}

export default Meal;
